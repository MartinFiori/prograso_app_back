auth.users
  └── profiles
        ├── events.created_by
        └── event_registrations.user_id

event_categories
  └── events
        └── event_registrations

event_statuses
  └── events.status_code

registration_statuses
  └── event_registrations.status_code